{
  "name": "glrc-eleventy-site",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "eleventy",
    "dev": "eleventy --serve --port ${PORT:-3000}",
    "serve": "eleventy --serve"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1",
    "@stackbit/types": "^2.1.15",
    "@stackbit/cms-git": "^1.0.38"
  }
}
